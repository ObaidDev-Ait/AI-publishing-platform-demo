# Frontend Layouts

Next.js App Router layouts live in `src/app/`:

- `src/app/(auth)/` — Authentication pages
- `src/app/(publisher)/` — Publisher dashboard layout
- `src/app/(admin)/` — Admin dashboard layout
- `src/app/layout.tsx` — Root layout

This folder documents the layout structure; route layouts remain in `src/app` per Next.js requirements.
